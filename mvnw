#!/bin/sh
echo "This is a placeholder Maven Wrapper script." >&2
echo "Run: mvn -N io.takari:maven:wrapper -Dmaven=3.9.9" >&2
echo "to generate the real wrapper, or install Maven and run 'mvn' directly." >&2
exit 1
